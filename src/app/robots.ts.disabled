// robots.txt disabled: serving /robots.txt was interfering with Google indexing.
// To re-enable: uncomment the block below and rename this file back to robots.ts.
//
// import type { MetadataRoute } from "next";
// import { siteUrl } from "@/lib/seo";
// export default function robots(): MetadataRoute.Robots {
//   return {
//     rules: {
//       userAgent: "*",
//       allow: "/",
//       // Allow crawlers to read page-level robots metadata, including noindex.
//       // disallow: allowIndexing ? ["/api/", "/admin/"] : ["/"],
//       disallow: ["/api/", "/admin/"],
//     },
//     sitemap: new URL("/sitemap.xml", siteUrl).href,
//   };
// }

